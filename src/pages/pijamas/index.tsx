// import { useEffect, useState } from "react";
// import type Pijama from "../../types/types";

export default function Pijamas() {

    

    // const cardsPorPagina = 12;
    // const [currentPage, setCurrentPage] = useState(1);
    
    // const [pijamas, setPijamas] = useState<Pijama[]>([]);
    
    // const start = (currentPage - 1) * cardsPorPagina;
    // const end = start + cardsPorPagina;
    // const visiblePijamas = pijamas.slice(start, end);

    // const totalPages = Math.ceil(pijamas.length / cardsPorPagina);

    return (
        <div>
            {/* <div>
                {visiblePijamas.map((item, index) => (
                    <div key={index}>{item}</div>
                ))}
            </div>

            <div>
                <button
                    onClick={() => setCurrentPage((p) => p - 1)}
                    disabled={currentPage === 1}
                >
                    Anterior
                </button>

                <span> Página {currentPage} de {totalPages} </span>

                <button
                    onClick={() => setCurrentPage((p) => p + 1)}
                    disabled={currentPage === totalPages}
                >
                    Próxima
                </button>
            </div> */}
        </div>
    );
}